{
  "Logging": {
    //A Log Provider is where we specify where our log messages get sent and what levels they may get sent at
    //"LogLevel" is the default provider for any Providers which haven't got a specific provider configuration like 'Console'
    "LogLevel": {
      "Default": "Error"
    },
    //Settings in Logging.Console.LogLevel override settings in Logging.LogLevel, where the Console is the provider name
    "Console": {
      "LogLevel": {
        "Default": "Warning" //This is 'Warning' only for the Console Log provider
      }
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultContainerConnection": "Server=cleanproject-database-server;Database=CleanDb;User=sa;Password=CApricorn5;TrustServerCertificate=true",
    "DefaultLocalhostConnection": "Server=localhost;Database=CleanDb;User=sa;Password=CApricorn5;TrustServerCertificate=true"
  }
} 